# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist/ NO se commitea: Hostinger recompila con `npm run build` y sus propias
# variables VITE_* (que se inyectan en build). Commitear dist local hornearia
# datos mock. El build estatico SPA sale en dist/client (ver DEPLOY-HOSTINGER.md).
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vercel (artefactos de build, no commiteables)
.vercel/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel-secret-tmp.txt